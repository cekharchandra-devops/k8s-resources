apiVersion: v1
kind: Pod
metadata:
  name: env
  labels:
    project: digital
    environment: dev
spec:
  containers:
    - name: nginx
      image: nginx
      env:
        - name: course
          value: devops
        - name: duration
          value: 120hrs